using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NextObstacleQueue : MonoBehaviour
{
    /*private Queue<GameObject> nextObstacleQueue;

    // Start is called before the first frame update
    void Start()
    {
        nextObstacleQueue = new Queue<GameObject>();

        // Find the initial obstacles (There are two)
        ObstacleHit[] initialObstacles = FindObjectsOfType<ObstacleHit>();
        nextObstacleQueue.Enqueue(initialObstacles[1].gameObject);
        nextObstacleQueue.Enqueue(initialObstacles[0].gameObject);
    }

    public void Enqueue(GameObject gameObject)
    {
        nextObstacleQueue.Enqueue(gameObject);
    }

    public GameObject Dequeue()
    {
        return nextObstacleQueue.Dequeue();
    }*/
}
